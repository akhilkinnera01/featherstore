apiVersion: v1
kind: Service
metadata:
  name: featherstore
  namespace: featherstore
  labels:
    app.kubernetes.io/name: featherstore
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/name: featherstore
