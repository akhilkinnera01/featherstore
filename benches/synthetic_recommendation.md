# Synthetic recommendation benchmark

Primary proof scenario: `single_get_request_qps` against `GET /v1/features/{entity}/{id}` with a fixed feature subset. Reports must distinguish HTTP request QPS from entity lookups/sec.
